import {createMuiTheme } from '@material-ui/core/styles';
const AppTheme = createMuiTheme({
    palette:{
        primary: { main: '#D4005E', contrastText: '#fff' },
        secondary: { main: '#FCF653', contrastText: '#fff'  },
        type:'dark'
    }
  });
  export default AppTheme